{
  "name": "caleclarke-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "dev:pages": "npm run build && wrangler pages dev dist",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy:pages": "npm run build && wrangler pages deploy dist --project-name=caleclarke-site"
  },
  "dependencies": {
    "astro": "^5.5.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
